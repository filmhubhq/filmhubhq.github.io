{
  "scripts": {
    "test": "echo \"Error: no test specified\" && exit 1",
    "dev": "netlify dev"
  },
  "dependencies": {
    "@sendgrid/client": "^7",
    "bootstrap": "^4",
    "jquery": "^3.6.0",
    "popper.js": "^1.16.0"
  }
}
