{
  "scripts": {
    "test": "echo \"Error: no test specified\" && exit 1",
    "start:lambda": "eval $(egrep -v '^#' .env | xargs) netlify-lambda serve src",
    "build:lambda": "netlify-lambda build src",
    "start:jekyll": "jekyll build --watch"
  },
  "dependencies": {
    "@sendgrid/client": "^6.3.0"
  },
  "devDependencies": {
    "netlify-lambda": "https://github.com/klausbadelt/netlify-lambda.git#patch-1",
    "serve-static": "^1.13.2"
  }
}
