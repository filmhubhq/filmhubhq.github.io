{
  "scripts": {
    "test": "echo \"Error: no test specified\" && exit 1",
    "start:jekyll": "bundle exec jekyll build --watch --incremental",
    "start:lambda": "eval $(egrep -v '^#' .env | xargs) netlify-lambda serve src",
    "build:lambda": "netlify-lambda build src"
  },
  "dependencies": {
    "@sendgrid/client": "6.4.0",
    "bootstrap": "4.4.1",
    "bs-custom-file-input": "^1.3.2",
    "jquery": "3.5.0",
    "lazyload": "^2.0.0-rc.2",
    "mixitup": "^3.3.1",
    "popper.js": "^1.16.0",
    "slick-carousel": "^1.8.1"
  }
}
